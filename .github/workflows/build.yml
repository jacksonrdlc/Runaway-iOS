name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v4

    - name: Setup Mapbox SDK authentication
      env:
        MAPBOX_SECRET_TOKEN: ${{ secrets.MAPBOX_SECRET_TOKEN }}
      run: |
        if [ -z "$MAPBOX_SECRET_TOKEN" ]; then
          echo "::error::MAPBOX_SECRET_TOKEN is not set"
          exit 1
        fi
        echo "machine api.mapbox.com" > ~/.netrc
        echo "login mapbox" >> ~/.netrc
        echo "password ${MAPBOX_SECRET_TOKEN}" >> ~/.netrc
        chmod 600 ~/.netrc
        echo "Mapbox authentication configured"

    - name: Select Xcode 16.2
      run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

    - name: Show Xcode version
      run: xcodebuild -version

    - name: Download iOS platform
      run: xcodebuild -downloadPlatform iOS

    - name: List available simulators
      run: xcrun simctl list devices available

    - name: Resolve Swift packages
      run: |
        xcodebuild -project "Runaway iOS.xcodeproj" \
          -scheme "Runaway iOS" \
          -resolvePackageDependencies

    - name: Build
      run: |
        xcodebuild -project "Runaway iOS.xcodeproj" \
          -scheme "Runaway iOS" \
          -destination "platform=iOS Simulator,name=iPhone 16,OS=latest" \
          -skipPackagePluginValidation \
          build
